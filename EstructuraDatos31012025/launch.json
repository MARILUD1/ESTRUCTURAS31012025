{
    "version": "0.2.0",
    "configurations": [
        {
            "name": ".NET: Launch",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build",
            "program": "${workspaceFolder}/bin/Debug/net7.0/EstructuraDatos31012025.dll", // Reemplaza con la ruta correcta a tu DLL
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "console": "internalConsole",
            "symbolOptions": {
                "loadAllSymbols": true
            }
        }
    ]
}